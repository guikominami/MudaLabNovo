import React from "react";

const BulletsList = ({ data_list }) => {

  console.log(data_list);

  return (
    <>
      <ul>
{/*         {data_list.map((item) => (
          <li>{item.name}</li>
        ))}   */}    
      </ul>
    </>
  );
};

export default BulletsList;